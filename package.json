{
  "name": "root",
  "private": true,
  "scripts": {
    "boot": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^6.0.3"
  }
}
